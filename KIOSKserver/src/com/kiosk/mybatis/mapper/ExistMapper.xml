<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Exist">
	<select id="select" parameterType="int" resultType="Exist">
		select * from exist where exist_id = #{exist_id}
	</select>
</mapper>